<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.tma24.keyboard">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.TMA24Keyboard"
        android:hardwareAccelerated="true"
        android:extractNativeLibs="true"
        tools:replace="android:allowBackup,android:icon,android:label,android:supportsRtl,android:theme,android:hardwareAccelerated,android:extractNativeLibs"
        tools:ignore="GoogleAppIndexingWarning">

        <!--
            Remove any MainActivity injected by library dependencies.
            Some versions of androidx.emoji2 and pl.droidsonroids.gif
            inject an internal activity without android:exported set,
            which causes the merger to fail on targetSdk 31+.
            tools:node="removeAll" tells the merger to strip every
            activity element named MainActivity from library manifests.
        -->
        <activity
            android:name="com.tma24.keyboard.MainActivity"
            tools:node="remove" />

        <!--
            Safety removal for any emoji2 internal FileProvider or
            activity that gets merged without android:exported.
        -->
        <activity
            android:name="androidx.emoji2.text.EmojiCompatInitializer"
            tools:node="remove" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.emoji2.text.EmojiCompatInitializer"
                tools:node="remove" />
            <meta-data
                android:name="androidx.lifecycle.ProcessLifecycleInitializer"
                tools:node="remove" />
        </provider>

        <service
            android:name=".TMA24InputMethodService"
            android:label="@string/ime_name"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <activity
            android:name=".SettingsActivity"
            android:label="@string/settings_title"
            android:exported="true"
            android:theme="@style/Theme.TMA24Keyboard.Settings">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
