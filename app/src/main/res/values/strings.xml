<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- ═══════════════════════════════════════════════════════════════
         BASE STYLES — required by AAPT as parents for child styles
         ═══════════════════════════════════════════════════════════════ -->

    <style name="KeyStyle">
        <item name="android:layout_margin">2dp</item>
        <item name="android:minHeight">40dp</item>
        <item name="android:gravity">center</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:textColor">@color/light_key_text</item>
        <item name="android:background">@drawable/bg_key_character</item>
    </style>

    <style name="FamilyStyle">
        <item name="android:layout_margin">2dp</item>
        <item name="android:minHeight">52dp</item>
        <item name="android:gravity">center</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:textColor">@color/family_btn_text_light</item>
        <item name="android:background">@drawable/bg_family_btn</item>
        <item name="android:textSize">24sp</item>
        <item name="android:fontFamily">serif</item>
    </style>

    <style name="ClipStyle">
        <item name="android:layout_margin">3dp</item>
        <item name="android:paddingStart">10dp</item>
        <item name="android:paddingEnd">10dp</item>
        <item name="android:paddingTop">4dp</item>
        <item name="android:paddingBottom">4dp</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:textColor">@color/light_chip_text</item>
        <item name="android:background">@drawable/bg_chip</item>
        <item name="android:textSize">13sp</item>
    </style>

    <!-- ═══════════════════════════════════════════════════════════════
         CHILD STYLES — inherit from their base and override as needed
         ═══════════════════════════════════════════════════════════════ -->

    <!--
        KeyStyle.Character
        Standard letter and symbol keys across all three keyboard panels.
    -->
    <style name="KeyStyle.Character" parent="KeyStyle">
        <item name="android:layout_margin">2dp</item>
        <item name="android:minHeight">40dp</item>
        <item name="android:gravity">center</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:background">@drawable/bg_key_character</item>
        <item name="android:textColor">@color/light_key_text</item>
    </style>

    <!--
        KeyStyle.Special
        Functional keys: Shift, Backspace, Tab, Esc.
        Darker surface to visually separate from character keys.
    -->
    <style name="KeyStyle.Special" parent="KeyStyle">
        <item name="android:layout_margin">2dp</item>
        <item name="android:minHeight">40dp</item>
        <item name="android:gravity">center</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:background">@drawable/bg_key_special</item>
        <item name="android:textColor">@color/light_key_text</item>
    </style>

    <!--
        KeyStyle.Action
        Primary action keys: Space bar and Enter.
        Uses ledger-blue in light theme, brass in dark theme.
    -->
    <style name="KeyStyle.Action" parent="KeyStyle">
        <item name="android:layout_margin">2dp</item>
        <item name="android:minHeight">40dp</item>
        <item name="android:gravity">center</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:background">@drawable/bg_key_action</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!--
        KeyStyle.Mode
        Mode-switch pill buttons: EN, አማ, and the coding </> button.
        Rounded pill shape via bg_key_mode drawable.
    -->
    <style name="KeyStyle.Mode" parent="KeyStyle">
        <item name="android:layout_margin">2dp</item>
        <item name="android:minHeight">40dp</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingStart">10dp</item>
        <item name="android:paddingEnd">10dp</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:background">@drawable/bg_key_mode</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!--
        KeyStyle.Arrow
        Arrow and navigation keys in the coding keyboard bottom row.
        Shares the action key background colour.
    -->
    <style name="KeyStyle.Arrow" parent="KeyStyle">
        <item name="android:layout_margin">2dp</item>
        <item name="android:minHeight">40dp</item>
        <item name="android:gravity">center</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:background">@drawable/bg_key_action</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!--
        ClipStyle.Chip
        Clipboard history suggestion chips shown in the candidates strip.
        Horizontal pill shape with border via bg_chip drawable.
    -->
    <style name="ClipStyle.Chip" parent="ClipStyle">
        <item name="android:layout_margin">3dp</item>
        <item name="android:paddingStart">10dp</item>
        <item name="android:paddingEnd">10dp</item>
        <item name="android:paddingTop">4dp</item>
        <item name="android:paddingBottom">4dp</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:maxWidth">160dp</item>
        <item name="android:singleLine">true</item>
        <item name="android:ellipsize">end</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:background">@drawable/bg_chip</item>
        <item name="android:textColor">@color/light_chip_text</item>
        <item name="android:textSize">13sp</item>
    </style>

    <!--
        FamilyStyle.Button
        The 7 Ethiopic order buttons shown in the Amharic candidates bar.
        Large Ethiopic text, serif typeface, deep blue background.
    -->
    <style name="FamilyStyle.Button" parent="FamilyStyle">
        <item name="android:layout_margin">2dp</item>
        <item name="android:minHeight">52dp</item>
        <item name="android:gravity">center</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:background">@drawable/bg_family_btn</item>
        <item name="android:textColor">@color/family_btn_text_light</item>
        <item name="android:textSize">24sp</item>
        <item name="android:fontFamily">serif</item>
    </style>

</resources>
